# get help
terraform -help

# Check version
terraform version

# Init project
terraform init

# Validate config
terraform validate

# Format files in same dir
terraform fmt

# Run plan and show what will be changed
terraform plan

# create
terraform apply

# Remove what created
terraform destroy

# Not ask confirmation to create
terraform alpply -auto-approve

# Show state file
terraform state show

# Show list of resources
terraform state list

# Replace specified resource. Get resource from command state list
terraform apply -replace <resource>

# Show in variables.tf (default value), terraform.tfvars or inline given variables
# Example var.host_os show host_os valiable value. Exit just write exit.
terraform console

# Inline definition from file
terraform console -var-file=dev.tfvars"

# Inline variables. These inlines work with other commands also.
terraform console -var="host_os=osx"

# Conditionals test in console. Use it in config files.
var.host_os == "windows" ? "powershell" : "bash"

# Output defined in outputs.tf file for garher values during running. Example
output "dev_ip" {
  value = aws_instance.dev_node.public_ip
}

# Show output values
terraform output

More from https://spacelift.io/blog/terraform-commands-cheat-sheet
